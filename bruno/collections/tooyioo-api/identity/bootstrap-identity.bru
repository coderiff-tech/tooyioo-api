meta {
  name: bootstrap-identity
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/identities/bootstrap
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
}

body:json {
  {
    "externalIdentityProvider": "Google"
  }
}

script:post-response {
  bru.setVar("dynamicIdentityId", res.body.id);
}

settings {
  encodeUrl: true
  timeout: 0
}
