meta {
  name: initiate-user-onboarding
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/user-onboarding/initiate
  body: json
  auth: inherit
}

body:json {
  {}
}

script:post-response {
  bru.setVar("dynamicUserOnboardingId", res.body.userOnboardingId);
}

settings {
  encodeUrl: true
  timeout: 0
}
